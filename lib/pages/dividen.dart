import 'package:flutter/material.dart';

class Dividen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Dividen"),
            );
  }
}
